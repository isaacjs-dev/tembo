<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between gap-4"><div><p class="text-sm font-bold text-primary-dark">Conteúdo pedagógico</p><h1 class="text-3xl font-black text-duo-heading">Atividades</h1><p class="text-gray-600">Atividades on-line, impressas ou híbridas.</p></div>@if($canCreate)<a class="duo-button-primary rounded-xl px-5 py-3 font-extrabold" href="{{ route('activities.create') }}">Nova atividade</a>@endif</div></x-slot>
    <div class="mx-auto max-w-7xl space-y-4">@forelse($activities as $activity)<article class="rounded-2xl border-2 border-duo-border bg-white p-5"><div class="flex flex-wrap justify-between gap-3"><div><span class="rounded-full bg-violet-100 px-3 py-1 text-xs font-bold">{{ ucfirst($activity->status) }}</span><h2 class="mt-2 text-xl font-extrabold">{{ $activity->title }}</h2><p class="text-sm text-gray-600">{{ $activity->questions_count }} questões · {{ $activity->schoolClasses->pluck('name')->join(', ') }}</p></div>@if(in_array((int) $activity->id,$manageableIds,true))<a class="duo-button-secondary rounded-xl px-4 py-2 font-bold" href="{{ route('activities.edit',$activity) }}">Editar</a>@else<a class="duo-button-secondary rounded-xl px-4 py-2 font-bold" href="{{ route('activities.show',$activity) }}">Visualizar</a>@endif</div></article>@empty<div class="rounded-2xl border-2 border-dashed bg-white p-10 text-center">Nenhuma atividade cadastrada.</div>@endforelse {{ $activities->links() }}</div>
</x-app-layout>
