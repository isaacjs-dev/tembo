<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-bold text-primary-dark">Aprendizagem ativa</p><h1 class="text-3xl font-black text-duo-heading">Revisões</h1><p class="text-gray-600">Crie experiências interativas, importe JSON e acompanhe resultados.</p></div><a href="{{ route('revisions.create') }}" class="duo-button-primary rounded-xl px-5 py-3 font-extrabold">Nova revisão</a></div></x-slot>
    <div class="mx-auto max-w-7xl grid gap-5 md:grid-cols-2 xl:grid-cols-3">@forelse($revisions as $revision)<article class="flex flex-col rounded-2xl border-2 border-duo-border bg-white p-5"><div><span class="rounded-full px-3 py-1 text-xs font-bold {{ $revision->status==='published'?'bg-green-100 text-green-900':'bg-amber-100 text-amber-950' }}">{{ str_replace('_',' ',ucfirst($revision->status)) }}</span><h2 class="mt-3 text-xl font-extrabold">{{ $revision->title }}</h2><p class="mt-1 text-sm text-gray-600">{{ $revision->items_count }} itens · {{ $revision->attempts_count }} tentativas</p><p class="text-sm text-gray-600">{{ $revision->schoolClasses->pluck('name')->join(', ') }}</p></div><div class="mt-5 flex flex-wrap gap-2"><a class="duo-button-secondary rounded-xl px-4 py-2 font-bold" href="{{ route('revisions.edit',$revision) }}">Editar</a><a class="rounded-xl border px-4 py-2 font-bold" href="{{ route('revisions.report',$revision) }}">Relatório</a></div></article>@empty<div class="col-span-full rounded-2xl border-2 border-dashed bg-white p-10 text-center">Nenhuma revisão cadastrada.</div>@endforelse</div><div class="mx-auto mt-6 max-w-7xl">{{ $revisions->links() }}</div>
</x-app-layout>
