<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-bold text-primary-dark">Revisão</p><h1 class="text-3xl font-black text-duo-heading">{{ $revision->title }}</h1></div></x-slot>
    <div class="mx-auto max-w-3xl rounded-2xl border-2 border-duo-border bg-white p-6"><p class="text-gray-700">{{ $revision->description }}</p><div class="mt-5 grid grid-cols-2 gap-3"><div class="rounded-xl bg-sky-50 p-4"><strong>{{ $revision->active_items_count }}</strong><br><span class="text-sm">etapas</span></div><div class="rounded-xl bg-violet-50 p-4"><strong>{{ $revision->max_attempts }}</strong><br><span class="text-sm">tentativas</span></div></div>@if($attempts->isNotEmpty())<div class="mt-5"><h2 class="font-extrabold">Seu histórico</h2>@foreach($attempts as $attempt)<p class="mt-2 rounded-lg bg-gray-50 p-3">Tentativa {{ $attempt->attempt_number }} · {{ $attempt->status==='completed'?'Nota '.$attempt->score:'em andamento' }}</p>@endforeach</div>@endif<form method="POST" action="{{ route('student.revisions.start',$revision) }}" class="mt-6">@csrf<button class="duo-button-primary w-full rounded-xl px-6 py-4 font-extrabold">Iniciar ou continuar revisão</button></form></div>
</x-app-layout>
