<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-bold text-primary-dark">Revisão concluída</p><h1 class="text-3xl font-black text-duo-heading">Seu resultado</h1></div></x-slot>
    <div class="mx-auto max-w-4xl space-y-6"><section class="rounded-3xl bg-gradient-to-br from-sky-600 to-blue-800 p-7 text-white"><p class="font-bold">{{ $revision->title }}</p><div class="mt-4 grid grid-cols-3 gap-3"><div><p class="text-xs">NOTA</p><p class="text-4xl font-black">{{ number_format($attempt->score,1,',','.') }}</p></div><div><p class="text-xs">XP</p><p class="text-4xl font-black">+{{ $attempt->xp_earned }}</p></div><div><p class="text-xs">NÍVEL</p><p class="text-4xl font-black">{{ $profile?->level??1 }}</p></div></div></section>
        @if($revision->feedback_mode!=='hidden')<section class="space-y-3"><h2 class="text-xl font-extrabold">Correção</h2>@foreach($attempt->responses as $response)<article class="rounded-2xl border-2 bg-white p-5 {{ $response->is_correct?'border-green-300':'border-amber-300' }}"><p class="font-extrabold">{{ data_get($response->item_snapshot,'prompt','Item respondido') }}</p><p class="mt-2 text-sm">{{ $response->feedback }}</p><p class="mt-2 text-xs font-bold text-gray-600">{{ $response->points_awarded }} ponto(s)</p></article>@endforeach</section>@endif
        <div class="flex flex-wrap gap-3"><a class="duo-button-primary rounded-xl px-5 py-3 font-bold" href="{{ route('student.revisions.show',$revision) }}">Ver tentativas</a><a class="duo-button-secondary rounded-xl px-5 py-3 font-bold" href="{{ route('student.revisions.index') }}">Outras revisões</a></div>
    </div>
</x-app-layout>
